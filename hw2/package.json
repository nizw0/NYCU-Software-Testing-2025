{
  "name": "hw2",
  "type": "module",
  "scripts": {
    "coverage": "vitest run --coverage",
    "test": "vitest run"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.1.1",
    "vitest": "^3.1.1"
  }
}
